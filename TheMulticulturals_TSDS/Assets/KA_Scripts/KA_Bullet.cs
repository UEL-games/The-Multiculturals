using UnityEngine;
using System.Collections;

public class KA_Bullet : MonoBehaviour
{

    private void OnCollisionEnter(Collision collision)
    {
        //var hit = collision.gameObject;
        //var health = hit.GetComponent<KA_Health>();
        //if (health != null)
        //{
          //  health.TakeDamage(10);
        //}

//        Destroy(gameObject);
    }
}